---
trigger: always_on
---

<!-- AUTO-GENERATED from shared/rules/project-context.md. Do not edit here; edit the source and run `python scripts/sync_rules.py`. -->

# ai-ide-workflows 项目上下文

本项目是一个内容创作项目：围绕用 Cursor / Windsurf 等 AI IDE 构建解决现实场景的工作流，制作教程视频与 GitHub 配套资源。

## 核心定位

频道母题：**用 AI IDE 构建工作流，解决现实世界中的各类实际场景问题**。

每期内容尽量同时包含：

1. **操作层**：具体怎么做，包含步骤、Prompt、流程或演示。
2. **判断层**：什么时候该用 / 不该用，如何验收，以及 AI 在哪些点上做不好。

判断层必须如实呈现 AI 的短板，不回避失败点。这是本项目区别于炫技型、软广型 AI 内容的关键。

## 工具定位

- **Cursor**：主线工具，中国用户多，可控性强，步骤清晰可见。
- **Windsurf**：副线 / 对比工具。

关键判断：即使底层模型相同，IDE 设计也会显著影响体验，包括上下文注入、任务规划、错误恢复。

## AI 助手工作原则

- 如果用户的方向明显有问题，要直接提醒，不要顺着错误方向走。
- 内容架构优先于内容运营，先确定故事线再做漏斗优化。
- GitHub 是内容配套资源仓库，不是主产品；建站暂缓，内容优先。
- 详细策略优先参考 `Project Context (项目上下文).md`、`Content Plan (内容计划).md`、`Risk and Moat (风险与护城河).md`。

---

## 频道配置（工作流的可配置默认值 · Single Source of Truth）

> `shared/workflows/` 是**内容无关的通用模板**，不得内联写死频道/某期/某平台的具体值。凡是频道级偏好一律集中在本节，工作流正文只写「读频道配置里的 X」。换频道 / 换期只改这里，不动工作流本体。

### 频道定位（供 `02` / `04` / `10` / `12` 引用）
- **定位语（slogan）**：「不吹AI，真落地，真开源」。
- **固定视觉挂点（首尾场景，每期必做）**：开场 `intro_scene` 首镜的 `subtitle` 尾缀带定位语（用「｜」分隔）；收尾 `outro_scene` 末镜的 `headline` 用定位语作品牌收束卡。
- **口播只念一次**：定位语只在结尾品牌收束卡念出一次（与 `outro_scene` 品牌卡同帧），正文其余位置不重复念 slogan。

### 选题打分口径
- **母题匹配度**：候选选题与上文《核心定位》频道母题的契合程度。

### 内容主线与讲述人设（供 `04-script-draft` 引用）

> 口播文本的完整质量标准（结构五段式 / 语感硬指标 / AI 味黑名单 / 转场句式 / 自检清单）见 `shared/rules/voice-style.md`，本节只定频道级口径。
- **讲述人设（可选项，默认不入口播）**：口播默认不做自我定位、直接讲价值与方法；某期确需点人设时，只在开场点一次，且必须用**价值前置表达**——本频道口径是「用 Vibe Coding 低成本、高效率地把目标能力自动化：人讲需求 → AI 实现 → 人判断验收」；**禁止自谦式表达**入口播（「我没有前后端基础」「我不会写代码」这类自我定位不得念出，改说「这套流程不吃编程基础：会讲需求、会判断验收就能复制」）；后续段落不再逐节复述"心法/这一步的意义"这类元叙述。
- **主线结构**：先用大白话讲「怎么选这条技术路线」，再讲「怎么把本期能力搭出来」；开场用**收益/命题前置的选题式钩子**（如「如何用 Vibe Coding 快速完成 X？」+ 一个关键认知），结尾落到「会讲需求、会判断，就能复制这套」。
- **讲法纪律**：全程大白话，禁止把"范式/换一套心智模型"等抽象标签当讲法；对照轴落在工程化复用 / 强类型约束 / 可维护性 / 稳定性等真实差异，不得用"省人力、少敲键盘"等劳动量轴当卖点。
- **跨期纪律**：本期只聚焦本期能力，跨期主题只留一句指针、不展开成节；具体跨期分工以当期 `Content Plan (内容计划).md` 与 `content-library/PIPELINE.md` 为准，不在工作流模板里写死期号。

### 平台与格式默认（供 `06` / `09` / `10` / `11` / `12` 引用）
- **目标响度**：主发布平台推荐响度默认 **-14 LUFS**（B 站 / YouTube 口径），混音与质检以此为准，误差 ±1 LUFS。
- **成片规格**：1920×1080 @ 30fps（见 `ARCHITECTURE.md` §5.2）。
- **封面/切片平台**：默认覆盖 B 站(16:9)、抖音(9:16)、小红书(1:1)、知乎、YouTube；具体清单可按当期调整。

### TTS 默认引擎（供 `06-tts-synthesis` 引用）
- **按口播语言选择引擎**：中文口播默认优先「豆包 TTS」（中文情感自然）；英文/混合默认「OpenAI TTS」；离线/零成本场景用「Piper」。具体引擎清单见 `06` 工作流表格，本节只定默认优先级。
